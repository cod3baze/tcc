const ends = {
  logout: "encerra a cessão do end",
  stats: "exibe as infos | níveis do end",
  close: "termina o terminal",
  clear: "limpa as infos do status",
  nfc: "show the notfications of ends",
  service: "show the sercives | API | used for trends",
  feedback_send: "Send message trends { $message }",
  contact: "send message for other user { $user $message }",
};

const trends = {
  cat: "create new trends { $title $body }",
  cat_issue: "create new trends in issue section { $head $title $body }",
};

const SAILE = [ends, trends];
